[project]
name = "email-agent"
version = "0.1.0"
description = "A local email summary agent that reads QQ Mail and summarizes today's important events."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
email-agent = "email_agent.cli:main"

[tool.setuptools.packages.find]
include = ["email_agent*"]
